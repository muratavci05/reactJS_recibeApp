import React from "react";
import { Form } from "react-router-dom";

const Form = (props) =>{

    return(
        <form>
            <imput type="text"/>
        </form>

    )
}

export default Form;